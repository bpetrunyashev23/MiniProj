<Window x:Class="MiniProj.CombatScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniProj"
        mc:Ignorable="d"
        Title="CombatScreen" Height="933" Width="421">
    <Grid>
        <Image Source="C:\Users\soulk\Downloads\Group 3.png" HorizontalAlignment="Center" Margin="0,0,0,-5"/>
        <TextBlock x:Name="enemyHP" HorizontalAlignment="Center" Margin="0,591,0,0" TextWrapping="Wrap" Text="Username" VerticalAlignment="Top" FontFamily="Matura MT Script Capitals" Foreground="White" Width="264" Height="56" FontSize="30"/>
        <Button x:Name="Login" Click="Button_Click" Margin="130,670,130,149" RenderTransformOrigin="0.5,1.36" Opacity="0">
            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <Ellipse Fill="{TemplateBinding Background}"/>
                        <ContentPresenter/>
                    </Grid>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
